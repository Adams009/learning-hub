function reverseString(str) {
    return str.split('').reverse().join(''); //split the string into an array, reverse the array, and join the array back into a string
}

console.log(reverseString('adam'))